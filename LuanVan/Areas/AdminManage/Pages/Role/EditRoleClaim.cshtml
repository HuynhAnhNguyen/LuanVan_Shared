@page
@model EditRoleClaimModel
@{
    ViewData["Title"] = "Chỉnh sửa đặc tính cho role (Edit claim for role)";
    var currentCulture = Thread.CurrentThread.CurrentCulture.Name;
    Console.WriteLine(currentCulture);
    if (currentCulture == "vi-VN")
        ViewData["language"] = _localization.Getkey("Vietnamese");
    else if (currentCulture == "en-US")
        ViewData["language"] = _localization.Getkey("English");
    else ViewData["language"] = "";
}

@*<h2>@ViewData["Title"]: @Model.role.Name</h2>*@
<div class="breadcrumbs">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-6 col-md-6 col-12">
                <div class="breadcrumbs-content">
                    <h1 class="page-title">Role-Claim</h1>
                </div>
            </div>
            <div class="col-lg-6 col-md-6 col-12">
                <ul class="breadcrumb-nav">
                    <li>
                        <a asp-page="../Home/Index"><i class="lni lni-home"></i> Trang quản trị</a>
                    </li>
                    <li>
                        <a asp-page="Index" asp-area="AdminManage"><i class="lni lni-list"></i> Danh sách role</a>
                    </li>
                    <li>Chỉnh sửa đặc tính của role</li>
                </ul>
            </div>
        </div>
    </div>
</div>
<partial name="_StatusMessage" model="@Model.StatusMessage"></partial>

<div class="account-login section">
    <div class="container">
        <div class="row">
            <div class="col-lg-6 offset-lg-3 col-md-10 offset-md-1 col-12">
                
                <form class="card login-form" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="card-body">
                        <div class="title">
                            <h3>Chỉnh sửa đặc tính (claim) cho role</h3>
                            <p>
                                Chỉnh sửa đặc tính (claim) cho role : @Model.role.Name
                            </p>
                        </div>
                        <div class="form-floating">
                            <input asp-for="Input.ClaimType" class="form-control" aria-required="true" />
                            <label asp-for="Input.ClaimType">Tên claim <span class="text-danger">*</span></label>
                            <span asp-validation-for="Input.ClaimType" class="text-danger p-2"></span>
                        </div>
                        <div class="form-floating">
                            <input asp-for="Input.ClaimValue" class="form-control" aria-required="true" />
                            <label asp-for="Input.ClaimValue">Giá trị <span class="text-danger">*</span></label>
                            <span asp-validation-for="Input.ClaimValue" class="text-danger p-2"></span>
                        </div>

                        <div class="button">
                            <button class="btn" type="submit">Cập nhật</button>
                            <hr />
                            <a class="btn btn-danger" asp-page="./DeleteRoleClaim" asp-route-claimid="@Model.claim.Id">Xóa claim</a>
                            <hr />
                            <a class="btn btn-primary" asp-page="./Edit" asp-route-roleid="@Model.role.Id">Chỉnh sửa role @Model.role.Name</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
